[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fraude-mobile-money"
version = "0.1.0"
description = "Détection de fraude sur transactions Mobile Money avec PaySim"
requires-python = ">=3.12,<3.13"

[tool.setuptools]
packages = ["src"]